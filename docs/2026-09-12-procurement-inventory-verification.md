# 采购入库、库存与出库本地验证记录

## 验证范围与结论

初次验证基线为 `c3c48025c0aeac0b0ee2f06b050bd950e122ced9`，此前浏览器检查候选版本为 `a3e4d279e582ba09a95db97467be0c596608c9d6`。最终评审后新增开票并发保护、入库提交恢复、库存 PDF 和历史明细删除保护，修改了业务代码。以下早期检查作为历史证据保留；修正版整体本地验收尚未完成，须由主任务重新执行最终浏览器检查，本文不是已上线证明。

所有数据写入仅发生在测试临时数据库及 `/private/tmp` 的副本。没有部署 VPS/NAS，没有打开或修改生产数据库。

## 自动化结果

| 检查 | 命令/范围 | 结果 |
| --- | --- | --- |
| 完整 Python | `.venv/bin/python -m unittest discover -s tests -v`，PATH 包含 bundled Poppler | 882 项，880 通过、2 跳过，120.975 秒，退出码 0 |
| 完整 JavaScript | `node --test tests/js/*.test.js` | 23 通过，0 失败，0 跳过 |
| 新增集成测试 | `.venv/bin/python -m unittest tests.test_purchase_inventory_isolation -v` | 4 通过，含四类别子场景 |
| 可选 Excel 实际打印 | 单独启用 `PURCHASE_TEST_SOFFICE` 与 `PURCHASE_TEST_PDF_READER` 运行三行订单页脚测试 | 1 通过，5.461 秒；覆盖原材料、纸箱、外协 |
| 差异空白检查 | `git diff --check` | 通过 |

完整 Python 中跳过的两项分别为可选真实浏览器发货颜色检查、可选 Excel 打印集成；后者已按上表单独启用并通过，日志 `/private/tmp/task8-optional-print.log`。已有 `datetime.utcnow()` 弃用和 ResourceWarning 提示未在本轮修改。

日志：`/private/tmp/task8-full-python.log`、`/private/tmp/task8-js.log`、`/private/tmp/task8-focused.log`。

## 跨流程隔离与账实验证

新增测试使用真实 Flask HTTP 路由、权限会话、CSRF 与完整应用 SQLite 架构，不替换业务服务。预先保存非空产品库存、产品流水、客户、销售订单、财务发票、历史手工到货作为哨兵数据。

原材料、纸箱、外协、其他采购分别执行：合格入库 100；移库 40（原批次剩 60）；目标批次盘点为 38；更新为已开票；从目标批次领用 10（剩 28）；原样重试；作废两次（目标批次恢复到 38，只恢复一次）。随后尝试作废已发生下游操作的到货单，收到 409，余额不变。

在每个阶段检查：

- 所有非采购库存域表的完整逐行快照不变，包括产品余额、产品流水、客户发货/送货单、销售财务和历史手工到货。源采购明细及订单业务字段保持不变，只有订单到货状态和更新审计允许变化。
- 每个采购批次 `available_quantity = SUM(quantity_delta)`，并且不小于零。
- `purchase_inventory_invariant_errors()` 返回空列表。
- `PRAGMA integrity_check` 为 `ok`，`PRAGMA foreign_key_check` 返回空列表。

另对四类未被下游使用的到货单执行两次作废：每批次最终余额 0，恰好两条流水（入库、反向冲销），其他业务数据不变。

在 `/private/tmp/procurement-copy-*` 中通过 SQLite backup 创建已完成流程的临时数据库副本，执行两次完整 `app.init_db()`，验证完整 SQL dump 一致、完整性/外键/库存不变量通过，再在副本执行纸箱完整流程。副本随测试清理。

测试夹具中的历史到货先完成既有供应商迁移，再建立业务隔离基线。这是对正常启动行为的安排，不放宽采购入库的隔离要求。

### 测试有效性（RED / GREEN）

本轮没有新生产功能或修复，不伪称已有功能最初缺失。首次夹具运行暴露两个测试准备问题：订单更新账号属于合法审计字段；新插入旧到货夹具需要先完成既有迁移。修正夹具后四项集成测试通过。

随后仅在临时数据库注入两种真实写入故障，以证明断言能检测回归：

1. 采购入库流水触发产品余额加 1：四类别子场景全部被完整快照断言捕获。
2. 采购出库流水触发采购批次余额多 1：四类别子场景被预期余额/流水合计断言捕获（29 不等于 28）。

撤去故障注入后完整回归通过。故障脚本、触发器没有进入应用或提交。证据为 `/private/tmp/task8-red-isolation.log`、`/private/tmp/task8-red-balance.log`。

## 权限与单据

四类别通过真实无价格权限账号请求到货详情、库存页面和已作废出库详情，随后请求到货 XLSX/PDF、库存 XLSX、出库 XLSX/PDF。检查 HTML、所有 XLSX ZIP 部件和提取出的 PDF 文本均无价格字段、金额标签或已知敏感价；导出返回 `no-store`，XLSX 为横向 A4。

完整回归还覆盖无权账号拒绝、CSRF、跨类别、停用库位、整单回滚、并发/过期版本、超收确认、保存快照、重复提交、网络结果未知后的出库恢复、原材料数值单元格及固定条款隔离。

### 本轮代表性文档目视检查

样张目录：`/private/tmp/purchase-task8-qa.N8yeL9`。样张为合成测试资料，不是生产业务单据。

- 原材料订单和纸箱订单：各 3 行，XLSX、原生 PDF、XLSX 实际打印 PDF。
- 到货单：3 行订单与实物资料对照，无价格，XLSX、原生 PDF、XLSX 实际打印 PDF。
- 库存清单：3 批次，无价格，XLSX、XLSX 实际打印 PDF。
- 出库单：包含中文领用人、经办人、原物品/订单/批次/库位和公式样式纯文本，无价格，XLSX、原生 PDF、XLSX 实际打印 PDF。

使用应用原有 openpyxl/ReportLab 生成器，Artifact Tool 只读导入/检查并渲染五张工作表，LibreOffice 实际打印五个 XLSX，Poppler 渲染四个原生 PDF 和五个打印 PDF。逐页目视检查这九份单页输出：中文、列标题、尺寸、数量、日期、页脚均可读，未见裁切、重叠或方框字。原材料固定条款和独立备注完整，纸箱无固定条款；纯文本公式/标记没有执行。

工作表只读检查无公式错误。原材料长度、宽度、厚度和数量的真实数值/General 格式由完整回归直接检查。长备注续页、重复表头及长行价格续行留空由既有导出测试覆盖；第 5/7 项的长文档目视证据仍适用，本轮未改文档生成器。

## 此前交互式浏览器检查（修正版待重验）

2026-09-13，主任务在 `127.0.0.1:8018` 启动候选版本，使用 `/private/tmp/jiede-procurement-preview/data/preview.db`、独立写锁及独立临时资料目录。数据库只包含合成供应商、两个启用库位和四类别样例订单；没有读取或修改项目正式数据库、资料目录、NAS 或 VPS。

实际浏览器操作结果：

- 四类别入库页及分类标签可进入；原材料表单显示长、宽、厚，纸箱表单显示长、宽、高。供应商、订单和物品查询入口正常。
- 原材料订单先登记实际到货 20、合格 0，详情明确显示“合格数量为零，未建库存”；再以仅有入库权限的账号登记第二批实际 20、合格 18，形成独立库存批次，原订单累计到货变为 40。
- 在剩余数量 80 时填写实际到货 100，页面弹出超收二次确认；取消后数量、库位和编辑值仍保留，没有新增单据。
- 采购库存按供应商、精确批次和库位筛选成功；将 10 从 QA-A 移至 QA-B 后同时看到来源余额和目标批次，再把目标批次盘点为 8，并更新为“已开票”，页面均显示成功及最新数量。
- 从 QA-A、QA-B 两个批次分别领用 3 和 2，出库单保存领用人、只读经办人 `admin`、来源订单/批次/库位、逐行备注和总备注；历史列表可查询并进入详情。
- 单独选择余额 6 的批次填写出库 999，页面提示“出库数量不能超过当前可用数量”，且勾选、数量和领用人保持不变，没有生成第二张出库单。
- 仅有 `purchase_receipt` 的账号能从导航和后台卡片进入四类采购入库，不能进入采购库存；其入库表单和到货详情不显示单价。管理员仍可进入原产品库存和客户发货页面，原发货的按订单/按组装件页签正常。
- 整个验收过程浏览器控制台无 warning/error。出库作废、到货作废、整单回滚、过期版本与网络结果未知恢复由真实 HTTP/挂载前端自动测试覆盖；浏览器确认了相应操作入口和冲突提示，但未在图形界面重复执行破坏性作废。

## 已知限制

- 最终评审修正后的浏览器验收待主任务执行，不能沿用旧版本检查结果宣称整体通过。
- 出库历史显示最新 100 条匹配单据，需筛选查找。
- 发生移库、盘点或出库后到货单不允许直接作废，出库作废也不解除此保护。
- 升级前未保存的历史名称只能按首次迁移时关联值回填，无法重建过去未记录的值。
- 采购库存已补齐独立 PDF，与 XLSX 使用相同的当前筛选、快照和价格权限；原“仅有 XLSX”范围缺口已修复。
- 本地通过不意味着正式部署已获授权。

## 评审修正复验

修正了操作指南权限表：开票状态更新属于“采购到货及入库”，不是“采购库存调整”，与实际路由和现有权限测试一致。

进一步收紧订单隔离检查：所有采购订单的所有字段均进入完整快照。只有首次入库或首次到货作废时，对当张来源订单的预期状态、操作账号和时间做明确校验后，允许该行的三项状态/审计字段变化；其他订单始终完整比较。操作成功后立即重建完整基线，重复提交及移库、盘点、开票状态更新、出库、出库作废都不得改变任何采购订单字段。

新增两个故障回归：入库时错误修改无关订单的状态、更新账号或时间；移库时错误修改来源订单的状态和账号。修正前 2 项测试出现 4 个预期失败，修正后准确捕获隔离快照变化。故障只存在于测试临时数据库，不修改业务服务。

- 专项集成与库存路由：14 项通过（含现在的 6 项集成测试），7.648 秒。
- 完整 Python：884 项，882 通过、2 项既有可选跳过，123.025 秒，退出码 0。
- 完整 JavaScript：23 项通过，0 失败，0 跳过。
- `git diff --check` 与测试文件编译通过。

证据：`/private/tmp/task8-review1-red.log`、`/private/tmp/task8-review1-focused.log`、`/private/tmp/task8-review1-full.log`、`/private/tmp/task8-review1-js.log`。本轮未修改单据生成器，前述文档目视检查和单独通过的 Excel 打印检查仍适用；浏览器验收状态不因本轮修正而改变。

## 最终评审修正第 1 轮（浏览器重验待执行）

本节是上述历史证据之后的最新自动化与文档验证结果，不将旧版本浏览器检查算作修正版已通过。

- 开票状态更新必须携带有效库存版本。两个版本 1 请求中，先提交者成功，后提交者收到 409 和无价格泄露的当前状态/版本；原样重试仍返回原操作。HTML 保留用户选择并显示最新状态，需核对后再次提交。
- 入库网络结果未知时冻结实际发送的完整请求及标识，包括已确认超收的请求；修改表单后重试仍只发送原请求。只有服务端确认标识尚未保存的 `revision_safe` 冲突可允许重新编辑或采用预览。已用标识返回 `idempotency_key_used` 和原单链接，前端锁住提交；过期预览只可安全定位同订单/类别原单，不授权新写入。挂载前端和真实 HTTP 覆盖丢失响应、编辑、原单恢复、过期凭证、权限及 CSRF。
- 四类别采购库存增加 `/export.pdf` 和保留当前筛选的页面入口，沿用 XLSX 的筛选、保存快照与价格权限，所有响应使用 `private, no-store`。PDF 横向 A4，含原到货行备注，支持长备注完整续页和重复表头。自动测试同时比较 PDF/XLSX 的匹配行、改名后快照、零库存筛选及无权内容。
- 采购明细删除保护检查所有到货/库存历史，不因到货作废失效；两行订单的实际“入库→作废→删除历史行”收到明确校验错误，完整数据库快照不变。累计到货和订购数量限制仍只计算未作废到货。

RED 证据：开票过期覆盖专项先出现 2 个预期失败；缺失库存 PDF 的 4 项测试出现 7 个预期失败；目视检查发现日期/金额断行后新增专项并先失败，再调整 PDF 列宽使其通过。入库恢复和作废历史删除也分别先复现旧行为，再通过修正版回归。

最新验证：

- 完整 Python：895 项，893 通过、2 项既有可选跳过，126.252 秒，退出码 0。跳过项为既有发货颜色浏览器检查和可选 Excel 实际打印集成；本轮未改 XLSX 排版，前述实际打印证据保留。
- 完整 JavaScript：28 项通过，0 失败、0 跳过，退出码 0。
- 库存 PDF 与既有到货导出专项：17 项通过，3.444 秒；开票/库存专项 29 项通过，8.266 秒。
- 完整套件再次执行跨流程隔离、临时数据库副本初始化、完整性、外键及库存不变量断言。

日志：`/private/tmp/task8-final-full-python-latest.log`、`/private/tmp/task8-final-js.log`、`/private/tmp/task8-final-invoice-red.log`、`/private/tmp/task8-final-invoice-green.log`、`/private/tmp/task8-final-pdf-red.log`、`/private/tmp/task8-final-pdf-layout-red.log`、`/private/tmp/task8-final-pdf-layout-green.log`。

新 PDF 样张位于 `/private/tmp/purchase-final-inventory-pdf.WjxhKm`，全部为合成数据。使用应用生成器和 Poppler 实际渲染并逐页目视核对四类别各 1 页及长备注 11 页，共 15 页：中文、类别尺寸、保存快照、日期/金额完整单行、表头、页脚和最终“结束标记”可读，未见裁切或重叠。长备注精确文本拼接也由自动测试验证。沿用既有 PDF 编写上下文，没有重复执行操作标记。

四项评审问题已在代码层修正，未发现本轮范围内的其他已知缺陷；最终浏览器验收及整体完成状态仍待主任务重跑后确认。没有部署，也未读写生产数据库或资料。
