# 五项修改本地验收记录

日期：2026-09-12。代码验收版本：`46e2330`；本轮范围从 `ba9caf1` 开始，不包含分支更早的功能开发。

## 范围与数据安全

已完成发货收货资料默认值、生产订单汇总/明细、订单明细关联工艺进度、原材料采购模板和纸箱/外协订单式采购页面。使用说明见同目录 `2026-09-12-order-progress-purchase-forms-guide.md`。

只在现有隔离工作区开发，未合并旧主目录、推送、部署 NAS/VPS、发送邮件或改写业务数据库。浏览器验证使用临时演示数据库和正常登录账号；演示服务验证后已停止。用户 Excel 模板只读参考，未导入其中的示例业务资料。

生产迁移是幂等增量迁移：历史工艺卡的订单关联为空，不猜测旧数据对应关系。原有订单、采购明细和收货引用 ID 保持不变；生产进度不自动变动库存或发货。

## 自动化验证

最终完整测试在 `46e2330` 上运行：

```text
FONTCONFIG_FILE=/tmp/jiede-purchase-template.gLQiUF/fonts.conf \
PURCHASE_TEST_SOFFICE=/Users/derek/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/override/soffice \
PURCHASE_TEST_PDF_READER=/tmp/jiede-purchase-qa.uC8jtg/inspect_pdf \
/Users/derek/Documents/jiede-web/.venv/bin/python -W ignore::DeprecationWarning -m unittest discover -s tests

Ran 761 tests in 89.461s
OK (skipped=20)
```

即 741 项通过，20 项跳过。可选的三类采购单真实打印测试已启用并运行。上列前三行是测试环境变量，不是应用部署配置。

另行通过：采购前端 8 项测试；普通发货交互、过期规格及输入法、组装发货删除/焦点/输入法共 5 个前端场景。最终列宽修复另有管理员/只读账号的列数与宽度对应回归。

测试中的数据库回滚、提交后异常和文件恢复异常由现有故障注入用例故意触发，测试判定均通过；没有全局屏蔽应用错误日志。

## 页面验证

- 同号订单按客户正确分组。搜索单个配件后，明细仍展示整组全部三行；保存和取消编辑均保留查询条件。
- 浏览汇总/明细不创建工艺卡。显式开始跟进后，需求 200、累计完成 80 显示 40%；重复保存 80 不累加，数量审计和其他工艺保持正确，库存流水不变。
- 工艺卡展示订单身份、需求、工艺数量及备注；历史独立卡保留单独入口。只读账号没有开始/保存/增删工艺控件。
- 采购供应商切换自动更新六项资料，不覆盖手改收货地址。数量 3、单价 12.30 显示 36.90，清空单价显示“未录价”。只读无价格账号能看明细与导出入口，但看不到价格和修改/取消操作。
- 已修复实际复现的采购旧脚本缓存问题，并为发货及采购资源加入版本回归覆盖。

## 导出验证

使用纯导出函数生成独立样例：9 份 Excel（包含 3 份无价格文件）、6 份直接 PDF、6 份 Excel 打印生成 PDF。逐项检查名称、单位、收货信息、公司信息、前导零、数字/日期类型、公式安全和价格权限。

三类三行短单均为一页横向 A4。45 行长单的 Excel 打印各 4 页，现有 PDF 各 5 页，均完整包含明细和页尾。已查看三类短单、长单末页及直接 PDF 的渲染结果，未见裁切；Excel 长单重复明细表头。

本轮按要求优化的是采购 Excel 模板；采购 PDF 保留原排版并补全物品名称/单位，不承诺两种格式分页完全一致。

## 验证限制

- 19 项原有 PDF 文本测试因缺少 `pdftotext` 跳过，另 1 项旧的可选浏览器测试未启用。实际 PDF 内容和版面已用 PDFKit 文本检查及图片渲染单独验证。
- 旧的独立 `shipping_contrast.cjs` 在缺少 Playwright 时无法启动，未安装依赖或改用外部浏览器自动化；页面交互由应用内浏览器工具验证。
- 浏览器工具对名为 `recipient_phone` 的输入框存在读值/遮蔽限制：手动写入后也读为空，原有采购表单同样出现；普通发货动态电话框及采购详情纯文本可正常读出。组装发货电话的页面数据、完整初始化事件和保存逻辑均通过测试，但不将该受限工具观察当作电话输入框的目视验收。

## 审查与保留状态

三项实现任务均经过独立规格/质量审查，整体审查无严重或阻断问题。最终唯一列宽小问题在 `46e2330` 修复，限定范围复核确认已解决且无新问题。

功能验收时，代码及说明保留在 `codex/shared-products` 本地工作区，等待单独决定合并、推送或部署。本轮未作超出已确认设计的功能取舍。

## 本地主分支合并验收

2026-09-12，用户选择“1：合并到本地主分支”。`main` 从 `74deab9` 快进到已验收的 `753f745`，无合并冲突；没有拉取远程更新、推送 GitHub 或部署 NAS/VPS。

合并前重新运行完整测试：761 项，87.460 秒，`OK (skipped=20)`。合并后在主目录 `/Users/derek/Documents/jiede-web` 再次运行同一完整测试：761 项，88.539 秒，`OK (skipped=20)`，即 741 项通过、20 项环境相关跳过。采购前端 8 项及 5 个发货交互场景也在主目录通过。

合并前后 `data/manuals.db`、`manuals.db` 和 `.env` 的文件大小与修改时间一致。没有重启业务服务，也没有执行业务数据库迁移。已清理无未提交业务文件的 `shared-products` 开发副本及已合并的本地分支；其代码和全部提交保留在 `main`，其他工作区未清理。
