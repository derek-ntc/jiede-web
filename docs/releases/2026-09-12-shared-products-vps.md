# 2026-09-12 VPS 发布记录

- 发布版本：`b6128c5dd5d997bd6245b58a450bc6e59ced0db1`（业务实现 `5691581`，后续提交仅修复 PDF 测试对 CID 字体编码的兼容）。
- 目标：`ubuntu@jiede.nbliwan.com:/home/ubuntu/jiede-web`，`jiede-web.service`。
- 服务恢复：2026-09-12 10:45:35 CST。
- 本次直接发布隔离分支的已提交版本；本地主分支未合并，GitHub 未推送，NAS 未更改。后续开发应从已发布版本继续，避免重新部署旧主分支。

## 验证

- 本地全套：719 项，OK，20 项环境相关跳过。
- VPS 全套：719 项，OK，23 项浏览器/PDF 提取工具相关跳过。
- VPS 的前端检查使用发布目录中的临时 Node.js v22.23.2；未修改系统 PATH、服务配置或安装系统软件。
- 先在正式数据库副本演练，再停服备份、发布并迁移。
- 正式产品：404 → 387，归档合并 17 条，无合并冲突。
- 库存分产品/库位逐项对照一致，总量 24,265（库存表全部记录的合计，包含历史孤立记录）。
- 85 条产品订单、290 条库存流水、119 条组装发货项、70 条送货单项及工艺/材料等引用按映射核对一致。
- 78 个附件引用有效；全部 80 个图纸/上传目录文件与发布前备份逐字节一致。
- 21 个发布文件校验一致；公网 HTTPS 登录页 200；产品、库存、供应商、订单、发货页面 200；供应商页面新银行字段存在。
- 数据库完整性检查：ok。

## 完整备份与回退

发布目录（仅拥有者可访问）：`/home/ubuntu/jiede-web-release-5691581-kOuYCt`。

- `predeploy-full.tar.gz`：停服时的原代码、数据库、图纸、上传资料及项目配置；权限 0600。
- `predeploy.sqlite`：停服时通过 SQLite 备份接口生成的原数据库；权限 0600。
- `production-migration-report.json`：正式迁移核验结果。
- `jiede-release-manifest-5691581.json`：发布前后文件摘要。
- `release-tests-final.log`：服务器全套通过日志。

此版本迁移后已恢复线上写入，后续不能直接以旧数据库覆盖现有数据库。如需回退，必须先停写、备份当前状态，并评估上线后的新增业务。迁移归档是审计资料，不是一键撤销。

## 已有历史数据问题（未清理、未新增）

发布前后的外键检查均有 169 条既有孤立引用：普通发货 → 订单 2、订单 → 产品 17、计划发货项 → 订单 1、组装发货项 → 产品 51、库存余额 → 产品 17、库存流水 → 产品 81。本次不扩大范围修复或删除这些历史记录。
