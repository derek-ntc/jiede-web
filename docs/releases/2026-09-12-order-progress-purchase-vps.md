# 2026-09-12 订单跟进与采购页面 VPS 发布

- 用户请求：部署到 VPS。
- 发布代码版本：`9a2f7d6aa18450a704795c11b0bc22521fe9c5c0`，本地 `main` 已合并验收。
- 目标：`ubuntu@jiede.nbliwan.com:/home/ubuntu/jiede-web`，`jiede-web.service`。
- 服务恢复：2026-09-12 15:37:32 CST；停止/恢复约 3 秒。
- 本次按哈希清单替换 25 个应用文件；不覆盖线上 `.env`、数据库、图纸、上传目录或虚拟环境。
- GitHub 未推送，NAS 未变更，无业务邮件发送。

## 发布内容

发货收货资料默认值、生产订单汇总/明细、按订单明细填写工艺累计数量及进度、原材料采购模板和 Excel、纸箱/外协订单式采购页面。采购/发货资源版本号确保刷新后加载新脚本。

## 上线前验证

- 本地主目录合并后全套：761 项，741 通过、20 环境相关跳过。
- VPS 专用发布目录全套：761 项，209.940 秒，`OK (skipped=24)`，即 737 通过。
- VPS 采购前端 8 项及 5 个发货交互场景通过。使用上次发布目录已有的 Node v22.23.2，仅供测试，未安装或修改系统运行环境。
- 先通过 SQLite 只读备份接口复制正式数据库，再在隔离目录演练增量迁移。
- 演练确认 62 张原有表的全部原列、记录内容不变；新增字段只用于订单工艺关联和数量版本，历史工艺卡未自动关联订单。
- 隔离副本中已登录的只读页面检查：生产订单、生产跟进、发货操作、原材料采购列表/新建、纸箱新建、外协新建均返回 200。

## 正式发布与校验

停服后生成完整备份，再更新代码并迁移；启动服务前对正式数据库重复执行与演练相同的逐表保全和幂等校验。

- 62 张原有表原数据全部不变，包括 387 产品、85 订单、307 库存流水、2 历史工艺卡及 8 工艺步骤、7 采购订单及 12 明细。
- 80 个图纸/上传目录文件在迁移前后哈希一致，发布后再次与完整备份逐文件核对一致。
- 25 个线上应用文件均与发布清单匹配；5 个修改后的公网静态文件返回 200 且哈希与候选版本一致。
- 数据库完整性：`ok`。外键检查仍有 169 条历史孤立引用，与发布前一致，未扩大范围清理或新增。
- `jiede-web.service` 为 active，两名 worker 正常启动；公网 HTTPS 登录页 200，未登录访问订单和生产跟进返回 302 登录保护。

## 备份及回退边界

私有发布目录：`/home/ubuntu/jiede-web-release-9a2f7d6-r9Y2xs`（0700）。

- `predeploy-full.tar.gz`：约 11 MB，原代码、配置、数据库、图纸及上传资料；0600，归档检查通过。排除虚拟环境、既有 backups 和根 Python 缓存。
- `predeploy.sqlite`：约 1.4 MB，停服时 SQLite 备份；0600，完整性正常。
- `production-verification.json`：正式迁移保全检查及各表计数；0600。
- `manifest.json`：25 个文件发布前/后摘要和完整提交号。
- `release-tests.log`：服务器完整测试结果。
- `verify.py`、`deploy.py`：本次演练和发布辅助脚本，保留在私有目录。

服务已经恢复写入，不可直接拿旧备份覆盖当前数据库。如需回退，先停写并备份当前状态，评估上线后新增业务，再决定代码与数据库回退方案。部署脚本只允许在尚未尝试启动服务时自动恢复旧数据库。
